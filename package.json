{
  "name": "unbound-v3",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ unbound-finance/unbound-v3",
    "create-local": "graph create --node http://localhost:8020/ unbound-finance/unbound-v3",
    "remove-local": "graph remove --node http://localhost:8020/ unbound-finance/unbound-v3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 unbound-finance/unbound-v3"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
